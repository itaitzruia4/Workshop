import { createMuiTheme } from '@mui/material/styles';

const dark = createMuiTheme({
    palette: {
        text: {
            primary: "white",
            secondary: "white"
        }
    }
});

export default dark;